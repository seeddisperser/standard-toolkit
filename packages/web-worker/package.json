{
  "name": "@hypergiant/worker",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "files": ["dist/**"],
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "bench": "pnpm vitest bench --run --dir src",
    "build": "pnpm tsc",
    "dev": "pnpm tsc --watch",
    "lint": "pnpm biome lint ./",
    "test": "pnpm vitest --run --dir src"
  },
  "devDependencies": {
    "@hypergiant/typescript-config": "workspace:*",
    "@hypergiant/vitest-config": "workspace:*",
    "@vitest/coverage-istanbul": "^1.6.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "mitt": "^3.0.1",
    "typescript": "^5.4.5"
  },
  "$schema": "https://json.schemastore.org/package",
  "author": "https://hypergiant.com",
  "sideEffects": false
}
